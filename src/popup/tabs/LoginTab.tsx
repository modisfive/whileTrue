import React, { FC, useState } from "react";

const LoginTab: FC<{}> = () => {
  return (
    <div>
      <h1>로그인하기</h1>
      <button>Notion 로그인</button>
    </div>
  );
};

export default LoginTab;
